{{#extends 'base'}}

    {{#block 'content'}}
        <form method="POST">

            <div class="alert alert-danger">
                Are you sure you want to delete the following food record:
                <ul>
                    <li>Name: {{record.name}}</li>
                    <li>Date: {{date record.date 'DD/MM/YYYY' }}
                </ul>
            </div>

            <input type="submit" value="Confirm Delete" class="btn btn-danger"/>
            <a href="/food" class="btn btn-success">Cancel</a>
        </form>
    {{/block}}

{{/extends}}